/**
 * index.ts — 交互层（程序入口）
 *
 * 提供一个简单的 REPL（Read-Eval-Print Loop）命令行界面：
 *   - 读取用户输入
 *   - 调用 CodeAgent.run()
 *   - 输出结果
 *   - 循环直到用户输入 "exit"
 *
 * 特殊命令：
 *   exit  — 退出程序
 *   reset — 清空对话历史，开始新任务
 */
import 'dotenv/config';
//# sourceMappingURL=index.d.ts.map